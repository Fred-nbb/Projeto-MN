[project]
name = "metodos-numericos"
version = "0.1.0"
description = "Implementação de métodos numéricos para resolução de equações não lineares"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"